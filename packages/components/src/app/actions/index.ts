import { setKeyboardOpen } from './set-keyboard-open';
import { remountApp } from './remount-app';
import setAppState from './set-app-state';

export default {
  setKeyboardOpen,
  remountApp,
  setAppState,
}
